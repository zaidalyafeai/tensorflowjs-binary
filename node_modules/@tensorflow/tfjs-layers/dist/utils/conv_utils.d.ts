import { PaddingMode } from '../common';
export declare function normalizeArray(value: number | number[], n: number, name: string): number[];
export declare function convOutputLength(inputLength: number, fliterSize: number, padding: PaddingMode, stride: number, dilation?: number): number;
